{
  "name": "@shardis/node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shardis store node: engine, persistence, replication, hash ring, and wire protocol.",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.23.15",
    "typescript": "^5.7.2",
    "vite": "^7.0.0",
    "vitest": "^5.0.1"
  }
}
